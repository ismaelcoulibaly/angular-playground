import {ValidatorFn} from "@angular/forms";

// export function validEmail(regParam: RegExp): ValidatorFn{
//   return(control: AbortController): {[key: string]: any} | null =>{
//     const valid = regParam.test(control.value);
//
//     // return valid ? {valid: {value: conrtol.value}}
//   }
// }
